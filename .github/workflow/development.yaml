name: CI

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: chmod update
        run: chmod 777 ./deploy.sh
      - name: run deploy.sh
        run: ./deploy.sh
